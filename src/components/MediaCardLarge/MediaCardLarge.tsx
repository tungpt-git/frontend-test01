import { Box } from "@material-ui/core";
import React from "react";
import { IVideo } from "../../utils/types";

type Props = {
  video: IVideo;
};

export default function MediaCardLarge(props: Props) {
  return <Box></Box>;
}
